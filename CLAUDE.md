# Project Instructions

This project uses Java 21 and Spring Boot.

Use the Maven Wrapper for all Maven commands:

./mvnw

The project quality gate is:

./mvnw -B -ntp verify

Code, file names, comments, and commit messages must be written in English.

Do not introduce unrelated changes.